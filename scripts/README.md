# Scripts Documentation

This document provides a brief summary of the Python scripts in this directory.

## mavlink_message_generator.py

This script generates MAVLink messages.

## param_comm_tester.py

This script tests for parameter communication. It likely tests the ability to read and write parameters to a MAVLink-enabled device, which is crucial for configuring and controlling the vehicle.

## plot_servo_values.py

This script is used to plot servo values. It probably reads servo output values from a log file or a live connection and visualizes them, which is useful for debugging and analyzing the behavior of servos.

## reproducer.py

This script is likely used to reproduce a specific state or behavior in the drone. This could be for debugging a bug, by replaying a sequence of events or commands that led to a failure.

## send_msg.py

This script's purpose is to send MAVLink messages. It could be a general-purpose script for sending any kind of MAVLink message to a connected vehicle, useful for testing and manual control.
